[user]
	name = Aldor007
    eamail = aldor007@gmail.com
[diff]
    tool = vimdiff
[merge]
    tool = splice

[mergetool "splice"]
    cmd = "vim -f $BASE $LOCAL $REMOTE $MERGED -c 'SpliceInit'"
    trustExitCode = true

